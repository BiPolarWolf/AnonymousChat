# Matter.js в AnonymousChat

Подробное объяснение физической симуляции плавающих пузырей с сообщениями.

## Что такое Matter.js?

Matter.js — это JavaScript библиотека для 2D физической симуляции. Она позволяет создавать объекты (тела), которые взаимодействуют друг с другом по законам физики: сталкиваются, отскакивают, падают под действием гравитации.

В нашем проекте мы используем Matter.js для создания плавающих пузырей с сообщениями, которые красиво двигаются по экрану.

## Основные компоненты Matter.js

### 1. Engine (Движок)

```typescript
engine = Matter.Engine.create({
  gravity: { x: 0, y: 0 }
})
```

**Что это?** Мозг всей физической симуляции. Он рассчитывает, как объекты должны двигаться, сталкиваться и взаимодействовать.

**Гравитация { x: 0, y: 0 }** — мы отключили гравитацию полностью. Без этого все пузыри падали бы вниз, как в реальном мире. С нулевой гравитацией они парят в пространстве.

### 2. Render (Рендерер)

```typescript
render = Matter.Render.create({
  canvas: canvas.value,
  engine: engine,
  options: {
    width: window.innerWidth,
    height: window.innerHeight,
    wireframes: false,
    background: '#1e293b'
  }
})
```

**Что это?** Художник, который рисует все объекты на canvas.

- `canvas.value` — HTML элемент, на котором происходит отрисовка
- `wireframes: false` — объекты рисуются с заливкой цветом, а не каркасом из линий
- `background: '#1e293b'` — темно-серый фон

### 3. Runner (Запускатель)

```typescript
runner = Matter.Runner.create()
Matter.Runner.run(runner, engine)
Matter.Render.run(render)
```

**Что это?** Часы симуляции. Он постоянно обновляет движок (60 раз в секунду), заставляя физику работать в реальном времени.

## Создание объектов

### Пузыри с сообщениями

```typescript
const circle = Matter.Bodies.circle(x, y, 60, {
  restitution: 1,
  friction: 0,
  frictionAir: 0.1,
  density: 0.001,
  render: {
    fillStyle: '#3b82f6',
  },
  label: text
})
```

**Matter.Bodies.circle(x, y, radius, options)** — создает круглое тело.

**Параметры:**
- `x, y` — начальная позиция (случайная точка на экране)
- `60` — радиус пузыря в пикселях
- `restitution: 1` — упругость (1 = идеальный отскок без потери энергии)
- `friction: 0` — трение между объектами (0 = скользкие как лед)
- `frictionAir: 0.1` — сопротивление воздуха (замедляет движение)
- `density: 0.001` — плотность (очень легкие пузыри)
- `fillStyle: '#3b82f6'` — синий цвет пузыря
- `label: text` — текст сообщения (хранится в объекте)

### Стены (границы экрана)

```typescript
const wallOptions = { isStatic: true, render: { fillStyle: '#1e293b' } }
const walls = [
  Matter.Bodies.rectangle(window.innerWidth / 2, 0, window.innerWidth, 50, wallOptions),
  Matter.Bodies.rectangle(window.innerWidth / 2, window.innerHeight, window.innerWidth, 50, wallOptions),
  Matter.Bodies.rectangle(0, window.innerHeight / 2, 50, window.innerHeight, wallOptions),
  Matter.Bodies.rectangle(window.innerWidth, window.innerHeight / 2, 50, window.innerHeight, wallOptions)
]
```

**Что это?** Невидимые стены по краям экрана, чтобы пузыри не улетали за пределы.

- `isStatic: true` — стены неподвижны, их нельзя сдвинуть
- Четыре прямоугольника: верх, низ, лево, право

## Плавающее движение

```typescript
function applyFloatingForces() {
  bodies.forEach((body, index) => {
    const time = Date.now() * 0.00001
    const floatX = Math.sin(time + index) * 0.00007
    const floatY = Math.cos(time + index) * 0.00007
    
    Matter.Body.applyForce(body, body.position, {
      x: floatX,
      y: floatY
    })
  })
}
```

**Это магия!** Без этой функции пузыри бы просто застыли или медленно остановились.

### Как это работает?

1. **time = Date.now() * 0.00001** — текущее время, постоянно растет
2. **Math.sin(time + index)** — синусоида создает волнообразное движение
3. **+ index** — каждый пузырь имеет свою фазу (они не двигаются синхронно)
4. **0.00007** — очень маленькая сила, чтобы движение было плавным
5. **applyForce** — применяем силу к пузырю, заставляя его двигаться

**Результат:** Пузыри плавно покачиваются в разных направлениях, как будто плавают в воде или в невесомости.

### Почему sin и cos?

- `sin` и `cos` создают круговое/волнообразное движение
- Они всегда возвращают значения от -1 до 1
- Идеально для создания естественного, повторяющегося движения

## События Matter.js

### 1. beforeUpdate — применение сил

```typescript
Matter.Events.on(engine, 'beforeUpdate', applyFloatingForces)
```

Вызывается перед каждым обновлением физики. Здесь мы применяем плавающие силы к пузырям.

### 2. afterRender — отрисовка текста

```typescript
Matter.Events.on(render, 'afterRender', () => {
  const context = render.canvas.getContext('2d')
  
  bodies.forEach(body => {
    const pos = body.position
    context.fillStyle = '#ffffff'
    context.font = '17px sans-serif'
    context.textAlign = 'center'
    context.textBaseline = 'middle'
    context.fillText(body.label, pos.x, pos.y)
  })
})
```

**Зачем?** Matter.js рисует только фигуры (круги, прямоугольники), но не текст. Мы вручную рисуем текст поверх каждого пузыря после того, как Matter.js отрисовал сами пузыри.

## Взаимодействие с мышью

### MouseConstraint — перетаскивание пузырей

```typescript
const mouseConstraint = Matter.MouseConstraint.create(engine, {
  mouse: Matter.Mouse.create(render.canvas),
  constraint: {
    stiffness: 0.2,
    render: {
      visible: false
    }
  }
});
```

**Что это?** Позволяет захватывать и перетаскивать пузыри мышью.

- `stiffness: 0.2` — жесткость "пружины" (0.2 = мягкое перетаскивание)
- `visible: false` — линия между мышью и объектом невидима

### Клик по пузырю — лопание

```typescript
Matter.Events.on(mouseConstraint, 'mousedown', (event) => {
  const body = event.source.body;
  
  if (body) {
    popBubble(body);
  }
});

const popBubble = (body: any) => {
  Matter.Composite.remove(engine.world, body)
  const index = bodies.indexOf(body)
  if (index > -1) bodies.splice(index, 1)
  console.log("Пузырь лопнул!")
};
```

**Как работает:**
1. Слушаем событие `mousedown` (клик мыши)
2. Получаем тело (body), на которое кликнули
3. Удаляем его из физического мира (`Composite.remove`)
4. Удаляем из нашего массива `bodies`

## Жизненный цикл Vue

### onMounted — инициализация

```typescript
onMounted(() => {
  // Создаем engine, render, runner
  // Добавляем стены
  // Запускаем симуляцию
  // Создаем первый пузырь
  // Запускаем таймер для создания новых пузырей каждые 5 секунд
  intervalId = setInterval(createMessage, 5000)
})
```

### onUnmounted — очистка

```typescript
onUnmounted(() => {
  clearInterval(intervalId)
  Matter.Render.stop(render)
  Matter.Runner.stop(runner)
  Matter.Engine.clear(engine)
})
```

**Важно!** Всегда очищайте ресурсы, чтобы избежать утечек памяти.

## Параметры для экспериментов

Попробуйте изменить эти значения и посмотрите, что получится:

### Гравитация
```typescript
gravity: { x: 0, y: 0.5 }  // Пузыри будут медленно падать вниз
```

### Упругость
```typescript
restitution: 0.5  // Пузыри будут отскакивать слабее
restitution: 1.5  // Пузыри будут отскакивать сильнее (набирать энергию)
```

### Сопротивление воздуха
```typescript
frictionAir: 0.01  // Пузыри будут двигаться быстрее и дольше
frictionAir: 0.5   // Пузыри будут быстро останавливаться
```

### Сила плавания
```typescript
const floatX = Math.sin(time + index) * 0.0005  // Более активное движение
const floatX = Math.sin(time + index) * 0.00001 // Едва заметное движение
```

### Размер пузырей
```typescript
Matter.Bodies.circle(x, y, 100, {...})  // Большие пузыри
Matter.Bodies.circle(x, y, 30, {...})   // Маленькие пузыри
```

## Полезные ссылки

- [Matter.js документация](https://brm.io/matter-js/docs/)
- [Matter.js примеры](https://brm.io/matter-js/demo/)
- [Matter.js GitHub](https://github.com/liabru/matter-js)

## Резюме

Matter.js в нашем проекте создает:
- ✨ Плавающие пузыри с сообщениями
- 🎯 Физические столкновения между пузырями
- 🖱️ Перетаскивание мышью
- 💥 Лопание пузырей по клику
- 🌊 Волнообразное движение без гравитации

Все это работает в реальном времени с 60 FPS благодаря оптимизированному физическому движку Matter.js!
